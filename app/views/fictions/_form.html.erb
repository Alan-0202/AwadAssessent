<%= form_with(model: fiction, local: true) do |form| %>
  <% if fiction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fiction.errors.count, "error") %> prohibited this fiction from being saved:</h2>

      <ul>
        <% fiction.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <div class="field">
    <%= form.label :reviews %>
    <%= form.text_field :reviews %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.text_field :year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
